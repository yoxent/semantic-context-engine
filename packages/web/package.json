{
  "name": "@sce/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create sce-db",
    "db:schema": "wrangler d1 execute sce-db --remote --file=../schema.sql",
    "db:import": "tsx import.ts",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
